# Copy to .env, then change the password. .env is gitignored; this file is not.
# A teammate reads this to learn WHICH variables exist, never YOUR values.
MONGO_USER=app
MONGO_PASSWORD=change_me
APP_PORT=3000
