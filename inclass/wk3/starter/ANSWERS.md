# In-class — Week 3

**Due Tuesday, September 8, 11:59 PM.** Counts toward participation.

**Name:**
**RCS ID:**

Three questions. Answer in your own words — a short paragraph each is plenty. I am
looking for whether the idea landed, not for length.

---

### 1. The first run of Example 1 was red, and that was the point.

Before you changed a line of `Survey.php`, three tests failed. Explain what you learned
about `averageComfort()` from *reading the failures* — the assertion messages and the
`DivisionByZeroError` — that you could not have learned from reading the function. Then
say what a test *is*, in one sentence, now that you have used four of them as a spec.



### 2. Example 2 never mentions PHP.

The Playwright test fills a form and reads the page that comes back. It does not know
what language the server is written in, and it could not tell if you swapped PHP for
Node. Explain what that test proves that the four unit tests in Example 1 cannot — and
what the unit tests prove that it cannot.



### 3. Which would you rather have twenty of?

You could write twenty tests like Example 1 or twenty like Example 2. Each Playwright test
took about 150 ms; each PHPUnit test took a few milliseconds. Using the pyramid reading,
say which you would want more of, which fewer of, and why the ratio is not a matter of
taste.



---

**AI Use Statement** *(required)* — which tools, what for, what you changed. One or two
sentences. "I did not use AI" is a complete answer.

