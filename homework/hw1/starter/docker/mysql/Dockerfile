# TASK 1 — the database image.
#
# Start from the official MySQL image and bake your schema into it, the way
# Example 2 did:
#
#   1. FROM mysql:8.4
#   2. COPY init.sql into /docker-entrypoint-initdb.d/
#
# That directory runs once, on first initialization of an empty data directory.
# Change init.sql later and nothing happens until `docker compose down -v`.

# YOUR DOCKERFILE HERE
