# TASK 2 — the web image.
#
# Start from an official PHP + Apache image and add what this app needs:
#
#   1. Base it on php:8.3-apache
#   2. Install the PDO MySQL driver (Example 5 shows the command)
#   3. Set the working directory to Apache's document root
#
# You do NOT need a COPY line -- docker-compose.yml mounts src/ instead,
# so you can edit PHP without rebuilding. Ask yourself what that costs
# if you ever deploy this image somewhere else.

# YOUR DOCKERFILE HERE
